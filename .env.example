# Development server custom hostname (for Tailscale/custom domains)
# Example: VITE_CUSTOM_HOST=myserver.tail08f20.ts.net
# VITE_CUSTOM_HOST=localhost

# Allowed hosts (comma-separated, leave empty to allow all)
# VITE_ALLOWED_HOSTS=
